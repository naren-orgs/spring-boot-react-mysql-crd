server:
  port: 9192

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/sb
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      format_sql: true